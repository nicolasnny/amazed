/*
** EPITECH PROJECT, 2024
** AMAZED
** File description:
** Functions to find the shortest path
*/

static int create_queue(node_t *root)
{
    queue_t *q = malloc(sizeof(queue_t));

    q->root;
    q->next = NULL;
    return q;
}

void find_shortest_path(char **graph, node_t *root, node_t *goal)
{
    queue_t *queue = create_queue();
}
